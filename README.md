# ip-address-checker
Программа на Python с графическим интерфейсом, которая позволяет загружать список IP-адресов из файла, проверять их доступность с помощью ping, а также создавать и редактировать списки IP-адресов
